/**
 * 请求的公共参数
 * Only one default export allowed per module,不要加default
 */

export const commonParams = {
  g_tk:5381,
  format:'json',
  inCharset:'utf-8',
  outCharset:'utf-8',
  notice:0
}

export const option = {
  param:'jsonpCallback'
}

export const STATUS_OK = 0;
